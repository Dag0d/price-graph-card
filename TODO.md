# TODO

- Introduce explicit types for card configuration, timeline points, thresholds, editor data, and action configuration.
- Enable stricter TypeScript options incrementally instead of switching the entire project at once.
- Expand tests to cover card rendering, lifecycle timers, Home Assistant context updates, actions, and editor configuration round-trips.
