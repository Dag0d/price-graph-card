name: HACS Validation
run-name: HACS Validation ${{ github.event_name == 'workflow_dispatch' && inputs.bridge_run_token || github.ref_name }}

on:
  workflow_dispatch:
    inputs:
      repository_ref:
        description: Ref to validate
        required: true
        type: string
      bridge_run_token:
        description: Internal bridge dispatch correlation token
        required: true
        type: string

  push:
    branches:
      - "main"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      REPOSITORY_REF: ${{ inputs.repository_ref || github.ref }}
    steps:
      - name: HACS validation
        uses: hacs/action@1ebf01c408f29afcb6406bd431bc98fd8cbb15aa
        with:
          category: "plugin"
          ignore: "images"
